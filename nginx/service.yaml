apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: pet-store-ns
spec:
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31322
  type: NodePort
