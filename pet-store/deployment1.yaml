apiVersion: apps/v1
kind: Deployment
metadata:
  name: pet-store1
  namespace: pet-store-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pet-store1
  template:
    metadata:
      labels:
        app: pet-store1
    spec:
      containers:
        - name: pet-store1
          image: pet-store-image:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          env:
            - name: NINJA_API_KEY
              valueFrom:
                secretKeyRef:
                  name: ninja-api-secret
                  key: api-key
            - name: MONGO_URL
              value: "mongodb://mongodb-service:27017"
            - name: STORE_COLLECTION
              value: "petstore1"
